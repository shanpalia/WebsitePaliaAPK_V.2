{
  "name": "paliaapk-hub",
  "version": "1.0.0",
  "private": true,
  "description": "PaliaAPK HUB Android App",
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "android:build": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "@capacitor/android": "^8.4.2"
  }
}
